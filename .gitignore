# Secrets — never commit
.env
.env.*
!.env.example
**/.env
**/.env.*
!**/.env.example

# Node
node_modules/
dist/
out/
release/
*.log

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Supabase local dev
supabase/.temp/
supabase/.branches/

# Editors / OS
.DS_Store
.idea/
.vscode/

# Claude local settings + bundled skills mirror (regenerable tooling, not app code)
.claude/settings.local.json
.claude/skills/
metrics/.venv/

# Cross-tool agent skills mirror (redundant copy of .claude/skills for non-Claude
# agents; regenerable, so it stays local). AGENTS.md itself is a symlink to
# CLAUDE.md and IS tracked, so any agent gets the same canonical instructions.
.agents/

# Local brainstorming mockups and browser interaction state
.superpowers/

# Internal working files — kept local, not part of the public repo
/CLAUDE.md
/AGENTS.md
/TODO.md
/docs/superpowers/
/docs/zone2-fitness-model.md
/docs/zone2-model-explained.md

# Knowledge library — a separate private repo, cloned in locally
knowledge/

# Personal RunKeeper export dumps (raw GPS traces — imported via
# scripts/import_runkeeper.py, never committed). Matches the export folder's
# name and any copy placed under data/.
*runkeeper-data-export*/
/data/runkeeper/
